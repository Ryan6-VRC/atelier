# MochiFitter — cross-base outfit refit

MochiFitter (もちふぃった～, BOOTH, by Nine Gates / おもちのびる) warps a garment authored for one base body onto a different one whose topology no scale or bone op bridges. It is a purchased vendor tool and is not redistributable: treat it and each conversion profile as detected dependencies, and fail loud naming what is absent rather than degrading. The **`mochifit`** skill owns the refit process end to end — install, route, drive, own, verify; this doc holds the tool facts it runs on.

**Reach for it only when a transform cannot.** `reproportion` handles topo-preserving base changes through equivalency edges and routes the genuinely non-topo case here — that boundary is declared there. A refit takes minutes and bakes geometry; an equivalency edge does neither.

## Modes — read the live one before every fire

**The window has four modes and defaults to the one the rest of this doc does not describe.** `ToolMode` (ver.64) is `OutfitRetargeting` (the Blender backend), `BlendShapeAddition`, `SuperMochiFitter` (the native `SuperMochiFitter/Editor/OutfitRetargetingSystem.exe`), and `ProfileGeneration` (beta); the two exe-backed entries appear in the popup only when their executable is present. The window's field initializes to `SuperMochiFitter`, and nothing persists it — no mode is stored in `OutfitRetargetingSettings.asset` and the field carries no `[SerializeField]`, so a domain reload returns the window to that default however you left it. It is a private member: reach it by reflection like every other internal, and re-read it per fire rather than once per session.

**The two retargeting backends deliver different artifacts.** `OutfitRetargeting` writes a plain FBX plus a reconstructed prefab depending on nothing under the tool's folder. `SuperMochiFitter` writes a `.omochi` into `Outputs/`, and the prefab reconstructed from it resolves through the vendor's `OrsScriptedImporter` inside `OutfitRetargetingSystem.dll` — so **that prefab** fails the independence gate `mochifit` enforces. The geometry does not: a `.omochi` opens in Blender through the addon the tool ships, and an FBX exported from there is as independent as any other (§Fit quality is not ours to judge). The native backend is also faster by roughly an order of magnitude — seconds against minutes — and is the only mode that converts meshes not sourced from an FBX, which the Blender modes refuse outright. Both carry a source prefab's Modular Avatar seam through (measured, ver.64), so neither forces you to re-author one.

**Choose by what happens next, not by the gate.** A hands-off independent asset with no touch-up wants `OutfitRetargeting`: its extra minutes buy the Blender trip you would otherwise make by hand. Touching the result up in Blender anyway wants `SuperMochiFitter`, strictly — the trip that fixes the fit delivers independence on the way past. Iterating on route or shape selections wants `SuperMochiFitter` too, and this is where the speed gap decides something rather than merely saving time: a selection retried in seconds is a workflow, one costing minutes is a single shot you live with. What is never a choice is the mode the window was left in — read it per fire.

The Blender backend's progress bar moves non-monotonically (0.56 → 0.79 → 0.83 → done), so a frozen-looking read is not evidence of a stall; a live `blender.exe` burning CPU is the discriminator.

## What a run needs

Per target base, a **conversion profile**: `avatar_data_<base>.json`, `config_<src>2<dst>.json`, `posediff_*.json`, `pose_basis_*.json`, and the `deformation_*.npz` fields. The tool installs at its native `Assets/OutfitRetargetingSystem/` and discovers available conversions by scanning its own `Editor/` folder, so a relocated tool or a profile installed anywhere else is invisible to it. Profiles ship apart from the avatar they dress as often as with it, and an asset library can shelve them under more than one root — **sweep the whole library by profile filename before declaring one absent**, or you will fail loud on a profile you own.

**Blender is a dependency of the Blender-backed modes only, and the tool installs its own per Unity project.** The retarget script runs on a Blender that MochiFitter downloads from Blender's own release server into `<ProjectRoot>/BlenderTools/`, outside `Assets/` — it does not ride in the `.unitypackage`, and no Blender already on the machine satisfies it (`docs/blender.md`'s pipeline build is a different version and is never consulted). Detection is a bare existence check on that path, so **a fresh venue re-incurs the download however many refits the machine has already run** — 382 MB over the network, not a click. The `Blender Status:` row and its `Download & Install` button render only in `OutfitRetargeting` and `BlendShapeAddition`; the row reports installed-or-not and no version, and ver.64 pins Blender 4.0.2 in the folder name it checks. `Execute Retargeting` greys out on whichever backend the current mode needs — the exe under `SuperMochiFitter/`, `BlenderTools/` for the other two.

**The downloader can wedge, and its progress reads "nearly done" while it does.** It fetches 20 MB byte-ranges, 8 in flight, into `%TEMP%\blender_chunk_<offset>.tmp`, under a one-hour client timeout with no per-chunk retry: one stalled chunk hangs the whole install at a frozen `downloadProgress` (measured: `isDownloading=True` at 99.49 % for 15+ minutes, three of twenty chunks short by 621 / 674 / 717 KB). Sum the chunk files against their offsets to tell a stall from progress. The remedy is to fetch the same release zip yourself with `curl` and extract it to the path the tool checks — 382 MB in 4.5 s, measured, after which the tool's own detection flips true.

**Profiles are directional, and most ship one way only.** `template2<X>` dresses X; `<X>2template` undresses it. Confirm the direction you need exists before promising a conversion. Routing is hub-and-spoke through a neutral `template`, so any base pair is two hops and the tool composes them itself.

**Derive the conversion graph from each config's contents, never from its filename** — `baseAvatarDataPath` is the destination, `clothingAvatarDataPath` the source. Shipped filenames contain typos.

## Traps

**The two avatar fields are not source-and-target in the obvious sense.** Target Avatar is the body being dressed. Source Avatar is the *route*, shown as `X → Y (Template経由)`. Setting the target alone leaves the route pointing somewhere unrelated, and the config field then reflects that wrong route — a run configured this way succeeds and produces the wrong conversion.

**The selected route's display name is not authoritative.** It can name one source while the config path the window resolved names another, so gate on the resolved path rather than the label. After a fire the `_temp.json` filenames are the receipt: they name the hop configs actually consumed, which is the only reading that survives a route set by a caller the window did not drive.

**The OMOCHI checkbox is an input-format switch, not the mode.** It renders only outside `SuperMochiFitter` mode — which forces the same format internally — and where it renders it changes the solver's *input* to a transient `.omochi` (written under the project's `Temp/`, never `Assets/`) and drops the hips-position argument, never the output format. `OutfitRetargeting` writes a plain FBX plus reconstructed prefab with **zero dependencies on the vendor's assets** either way (measured via `AssetDatabase.GetDependencies`, ver.64), and both survive uninstalling the tool. One measured comparison (Beryl→Nouvelle, ver.64): geometrically equivalent (0.066 mm max baked-mesh deviation, identical bone offsets and blendshape lists), ~5 % faster with the option on. The vendor marks it experimental; nothing rides on the choice. The `~3×` vertex inflation to fear belongs to importing a `.omochi` by hand through the vendor importer, not to any in-run path — a `SuperMochiFitter` output measured 1.001.

**The bundled Beryl sample costume's geometry lives inside the tool**, at `Editor/TestingDatasets/Beryl_Costumes.fbx`; the root-level `Assets/Beryl/` the installer also drops holds only its four materials and six textures. Source a refit from the sample and it dies on the next tool re-import — copy the FBX out first.

**`avatar_data_<base>.json`'s `defaultFBXPath` is a picker default, not the live binding** — the actual base FBX is `OutfitRetargetingSettings.asset`'s `baseFbxAsset`, a GUID reference. A stale or absent `defaultFBXPath` still runs correctly, and a GUID reference scan won't catch it — don't read the JSON path as evidence a profile is broken.

**A run mutates the vendor folder.** Every fire rewrites `_temp.json` copies of configs and avatar data beside the shipped originals, with paths rewritten to chain the hops — their presence after a run is normal residue, not damage. The trap is a **failed** run's leftovers: the next run consumes them, and nothing distinguishes them from healthy residue — so **delete every `_temp.json` before firing**; they are disposable and regenerated per fire, never an input worth keeping.

**Completion pops a blocking native Win32 modal that stalls the Editor's tool queue** — title `Success`, a single `OK` button (measured, ver.64). Detect completion by watching the output folder for the finalized prefab; the Editor is unreachable until the dialog is dismissed. Two verified dismissal routes: `tools/unity-dialog.ps1` from outside the process (exact title + button per its doctrine), or pre-arm the vendor's own static result-dialog suppression flag on the window type — find it by reflection like every other member; its name is an internal and stays out of tracked files.

**`renderer.bounds` cannot verify placement.** The tool deliberately unifies every renderer to one global bounding box. Bake the skinned mesh and measure real vertex positions against the target body's bones.

## What a refit costs

Output is a **baked** garment plus a reconstructed prefab. Topology, UV layers and materials survive; the armature is replaced with the target's. Each source shape you select as a shape field drops — that selection is an input to the solve, so a wrong one is a silent fidelity loss, not an error. **What arrives in their place is the target `avatar_data`'s whole shape set, not the route's fields**: two selected fields yielded nine target keys plus an `MF_Origin` marker on all 11 meshes in one run; another landed all four of the target's fields with one of them declined. So a census diffed against the selection you made will not balance — and whether the source-side selection gates anything at all is undetermined from those runs.

**Bones the target base lacks are dropped.** A source rig carrying individual toe or breast bones, refitted onto a base without them, loses that weighting; the remaining weights renormalize, so nothing collapses and nothing warns. This is a property of the base pair rather than a fixable defect — surface it, don't chase it.

**Components on the source prefab do not all survive the warp.** `ModularAvatarMergeArmature` and `ModularAvatarMeshSettings` came through on a measured run; `ModularAvatarBlendshapeSync` did not — re-author it against the arrived shape names rather than assuming it rode along. The vendor's own readme warns that animations and complex gimmicks on the outfit frequently need rework too. Treat a refit as producing garment geometry, not preserved outfit behavior.

## Verifying one

Rest pose proves nothing here: a refit whose weight transfer failed still renders correctly and disintegrates the moment it is posed. Gate on humanoid-bone world-position coincidence before any render, then sweep poses per `docs/verify.md`. `CheckSeam.CheckBare` is that gate (`unity-tools.md`) — the raw output does carry a `MergeArmature`, but it has no base to resolve against yet, so the seam door lands on the `mergeTarget` abstain and the bare door is what scores two side-by-side skeletons (measured, on three outputs of two separate runs). Pass it the solver-noise tolerance the `mochifit` skill owns; measured residue on those runs was `maxWithinEps` 0.0011mm, so the skill's 0.01mm gate sits an order of magnitude clear. The seam door scores later, placed on the real base at compose.

## Fit quality is not ours to judge

Nothing here scores drape, clipping, or whether a refit *looks* right, and no door should be built to. Those are eye judgments, and a VR pass sees what a desktop screenshot does not. The gate above certifies the skeleton and stops there.

When a posed check looks wrong, the remedy is a Blender round-trip, and it is the operator's step rather than the tooling's. `OutfitRetargeting` already hands over an FBX. A `SuperMochiFitter` `.omochi` opens through the addon the tool ships at `Editor/OmochiFormat/BlenderAddon` — `import_scene.omochi`, installed by the window's own `InstallOmochiBlenderAddon` — carrying armature, weights, shape keys and material slots, and `avatarprep`'s gated `export_unity_fbx` accepts the imported scene unmodified.

**The trip discards the Unity components either way**, seam included, so the returning geometry re-enters `own-mergeable`'s seam-only own to have a seam authored — not `mochifit` step 5, which exists to confirm a seam that survived the warp.
